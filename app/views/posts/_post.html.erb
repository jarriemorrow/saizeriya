<div class="card w-full m-auto shadow-xl max-w-xs hover:bg-emerald-300">
  <%= link_to post_path(post), data: { turbo: false } do %>
    <div class="flex justify-start">
      <div class="avatar m-4">
        <div class="w-12 mask mask-squircle">
          <%= image_tag post.user.avatar_url %>
        </div>
      </div>
      <div class="my-auto">
        <%= post.user.name %>
      </div>
    </div>
    <div class="mx-4">
      <%= post.recipe_name %>
    </div>
    <div class="mx-4">
      <!-- 投稿画像のイメージが入るところ -->
      <%= image_tag post.user.avatar_url, class: 'rounded-xl w-72 h-72' %>
    </div>
  <% end %>
  <div class="flex justify-between">
    <div class="my-auto">
      <div class="m-4">
        <%= l post.created_at, format: :long %>
      </div>
    </div>
    <% if logged_in? %>
      <div class="m-4">
      </div>
    <% end %>
  </div>
</div>